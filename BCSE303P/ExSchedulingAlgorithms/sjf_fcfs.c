#include "stack.c"

struct process{

};

int size_limit_of_ready_stack = 10;
struct process *processes_in_system[10];

int counter;

struct process* scheduler();
    static running_process, counter
};

void preempt(int n, process args[]){
    signal overriding
    create process - dynamic memory allocation
}

void execute(struct process p, int *counter){
    while(counter){
        sleep();
    }
}


int main(){ // driver

}